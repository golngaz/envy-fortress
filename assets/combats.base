formulas:
  test: if(file.ext == "png", 'c\'est une image', 'rien à voir')
views:
  - type: table
    name: Table
    filters:
      and:
        - file.folder == "combats"
    order:
      - file.name
      - file.folder
      - formula.test
      - file.ctime
      - file.mtime
      - difficulty
    sort:
      - property: file.mtime
        direction: DESC
      - property: formula.test
        direction: DESC
    columnSize:
      note.difficulty: 121
